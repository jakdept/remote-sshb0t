---
remote_sshb0t:
  domains:
    - url: example.com
      github_api_url: "api.example.com"
      allowed_users:
        - bob
        - alice
        - octocat
      allowed_github_orgs:
        - partyhouse
      allowed_gitlab_groups: []
  default_authorized_keys: []
  overwrite: False
